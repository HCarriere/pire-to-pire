var search = require('./search');

module.exports = {
    findNews : search.findNews,
    findArticles : search.findArticles,
    findShareables : search.findShareables,
    findUsers : search.findUsers
}